# Dies ist eine *.txt Datei zum testen.
# Zum besseren Verstaendnis wird hier eine weitere Kommentarzeile eingefügt.


import 'irgendwas' from '../sonst/wo';

function getBees(name, count){
  var bees = [];
  for (var i=0, i<count, i++) {
    bees.append({
      name: name,
    })
  }

  return bees;
}
